# Let's create the Snake class. This is a child class of the Reptile class.
# Once again, we must import the parent class
from reptile import Reptile
class Snake(Reptile):

    # Let's initialise the class
    def __init__(self):
        super().__init__()

